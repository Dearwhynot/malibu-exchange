# Error-Log Module

Reusable WordPress admin module for viewing the standard `wp-content/debug.log`.

- Scope: debug-log viewing only
- Default viewer: `v2`
- Integration into Malibu runtime is intentionally not done at this stage
